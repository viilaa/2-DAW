Este proyecto esta creado para ver que diferencia de velocidad hay al instalar paquetes en nuestro proyecto con: ( es mucho mas rapido YARN)
    - Npm:
        npm i --save <nombre_paquete> (te pone el paquete en dependencies, dentro de package.json) 
        npm install -g <nombre_paquete> 
        npm install <nombre_paquete> (te descarga el paquete de forma temporal)
        npm i (te recupera los paquetes que tenías instalados dentro de la carpeta node_modules y aparte tambien el archivo package-lock.json)
    
    - Yarn:
